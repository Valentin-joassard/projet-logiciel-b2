using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class winnerGameOver : MonoBehaviour
{

    public Text winnertext;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    void Update()
    {
        //winnertext.text = GameObject.Find("Gamelogic").GetComponent<GameLogic>().winner.name + "a gagné";
    }
}
